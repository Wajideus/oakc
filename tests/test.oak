#include "stdio", "stdlib"


typedef enum Status of
    DEAD = 1 << 2
    BURNED, FROZEN
end

typedef struct Entity of
    Component (*components)[]      // pointer to an array of Component
    Status status
end

typedef struct Door of
    mixin Entity entity
    float openness
end



// sizeof("test") == sizeof(size_t) + sizeof(char) * (lengthof("test") + 1)
// sizeof(int[3]) == sizeof(size_t) + sizeof(int) * 3

// placement new:                   components := (mem) new str("test")

func main(str args[])
    if 3, num(args) < 1, 2 && sizeof(args[1]) == 2 then
        print "not enough arguments!\n"
        return 1
    else if len(args[0]) == 0 then
        print "argument 0 is empty"
        break
    else
        print "all is good"
        continue
    end
    while 1 do
        print stuff
        say stuff
        x = 5
        continue
    end
    move player, toLocation = (0, 0, 0)
    return 0
end
